# Autonomous session 2026-06-30: findings while you were away

## Headline: our max_6 result is NOVEL and now bulletproof

Read the literature properly (arXiv:2505.14338, Bakaev-Brunck-Hertrich-Stade-Yehudayoff, STOC 2026):
- They prove `max_5` is 2-hidden-layer (explicit 9-term signed sum of weight-2 joins, via polyhedral
  subdivision of the simplex + the valuation identity `(P u Q)+(P n Q)=P+Q`).
- They **explicitly leave `max_6` open**.
- The best general upper bound is `ceil(log3(n-2))+1` (= 3 for n=6,7), and the only real/decimal lower bound is
  `ceil(log3 n)` (Averkov-Hojny-Merkert, decimal weights), which forces 3 layers only at `n >= 10`.

**Our `max_6` IS 2-layer (6-orbit signed rep, weight-2) resolves their open question.** Re-verified two ways:
exact-rational solve with 0 error over 400 fresh rational points (verify_2layer.py), AND the rigorous
arrangement-cell proof (check_max6.py: 0 of 2608 cells have the wrong gradient). This is a clean, publishable,
coauthorship-grade contribution on its own. Writeup already exists: WRITEUP.md.

## Reframed hypothesis: max_7, max_8, max_9 are probably 2-layer; n=10 is the first 3-layer case

(IN_HYPOTHESIS.md) Every known depth lower-bound technique is a base-`b` volume/parity argument that forces
3 layers only around `n = b^2`: base-2 (integer, HHL) and base-3 (decimal, AHM) bottom out at `n ~ 9-10`. **No
known technique gives a real/rational lower bound > 2 at n=7.** Combined with constructions existing for n=4,5,6,
the natural picture is: 2-layer for `n <= 9`, first 3-layer at `n = 10` (where the decimal bound becomes tight).
This also explains why every OUT obstruction we built collapsed -- there may be nothing to obstruct.

This FLIPS the earlier "separation probably holds" lean. Honest status: it's a hypothesis from the shape of known
results, not a proof. The decisive test is to CONSTRUCT a 2-layer max_7 (it must use edge complexity >= 4, since
max_7 is proven OUT of weight-2 and complete weight-3).

## The frontier and the wall

`max_7` IN-vs-OUT is genuinely unresolved and tool-walled both ways:
- IN: needs a weight-4 construction. The complete weight-4 family is ~10^6-10^7 orbits (165529 zonotopes alone)
  -- too large to enumerate/store. Structured subfamily probes (weight4_modp.py, weight4_orbitsum.py) can only
  FIND IN, never certify OUT. The reliable LS floor over [complete weight-3 + structured weight-4] is ~0.0006
  (not ~0), which leans against a LOW-complexity weight-4 rep -- but is inconclusive (subfamily + the cusolver
  DLL is missing so only iterative CGLS works, and it is very ill-conditioned/slow).
- OUT: would be the first nontrivial real-weight depth lower bound for any max_n. Beyond all known techniques;
  every continuous/linear/intersection/homological obstruction we tried provably collapses (CONSOLIDATED.md).

## Reliable methodology (durable; fixes months of flaky runs)

- The "timeouts" were a SELF-IMPOSED `timeout 595` wrapper; background tasks ignore it. Drop it -> runs complete.
- Guards that matter: m > rank (non-vacuous), CGLS run to a real plateau (it is SLOW: f(3) still dropping at
  78000 iters; true f(3) ~ 0.0006, not the 0.00159 we'd quoted), random-target CONTROL floor for vacuity,
  per-stage caching, root-vs-non-root family checks. cusolver is unavailable -> no GPU eigh/svd/qr; matmul only.
- Reliable floor trajectory (control-validated, but CGLS-under-converged upper bounds): f(2)=0.0308 (exact),
  f(3) ~ 0.0006, f(4) <= ~0.0005. Monotone, leans closure.

## Concrete next steps (in priority)

1. Settle max_7 by CONSTRUCTION: derive the valuation/polyhedral-subdivision cover of Delta_7 by joins (the
   technique that gave max_5/max_6). If found -> max_7 IS 2-layer (novel, extends the frontier; supports n=10
   threshold). This is analytic work, not enumeration -- the right attack.
2. If construction resists: try max_8, max_9 too (the n=10 threshold story needs 7,8,9 IN + the n=10 decimal
   bound, which is already proven).
3. Package max_6 (DONE) + the max_7,8,9 program as the Hertrich-group collaboration pitch.
